# 20 — Preset logs-of-event query (JS)

One-shot `presetQueryLogsOfEvent` for USDT Transfers in a small block range.

**Concepts:** preset helpers, bounded `get()`, event topic filters.

```bash
export ENVIO_API_TOKEN=...
npm install
npm start
```

Expected output reports matching logs and `nextBlock`. Try another address, topic, or small block range in `main.js`.
