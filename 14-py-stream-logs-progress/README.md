# 14 — Stream contract logs with progress (Python)

Stream USDT Transfer logs and print batch progress. Bounded by `MAX_BATCHES`.

```bash
export ENVIO_API_TOKEN=...
pip install -r requirements.txt
python main.py
```
